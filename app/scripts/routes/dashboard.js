/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var DashboardRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return DashboardRouter;
});